name: Deploy Survey Frontend to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'packages/survey-frontend/**'
      - 'packages/sdk/**'
      - '.github/workflows/deploy-survey.yml'
  workflow_dispatch:

# 允許寫入 GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# 只允許一次部署
concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    name: 建置 Survey 前端
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🔨 建置 SDK（Survey 依賴）
        run: |
          cd packages/sdk
          pnpm run build

      - name: 🏗️ 建置 Survey 前端
        working-directory: packages/survey-frontend
        env:
          VITE_API_BASE: https://pos-backend-staging.survey-api.workers.dev
          VITE_LIFF_ID: ${{ secrets.VITE_LIFF_ID }}
        run: |
          # Debug: 顯示環境變數
          echo "Environment variables check:"
          echo "VITE_API_BASE: ${VITE_API_BASE:0:20}..."
          echo "VITE_LIFF_ID: ${VITE_LIFF_ID:0:15}..."
          
          # 建立 .env.production 檔案
          echo "VITE_API_BASE=${VITE_API_BASE}" > .env.production
          echo "VITE_LIFF_ID=${VITE_LIFF_ID}" >> .env.production
          
          # 驗證檔案內容
          echo "Checking .env.production:"
          cat .env.production
          
          # 執行建置
          pnpm run build:gh-pages
          
          # 驗證建置產物中是否包含 LIFF ID
          echo "Checking if LIFF ID is in bundle:"
          grep -r "${VITE_LIFF_ID}" dist/assets/*.js && echo "✅ LIFF ID found in bundle" || echo "❌ LIFF ID NOT in bundle"

      - name: 📤 上傳建置產物
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/survey-frontend/dist

  deploy:
    name: 部署到 GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 🚀 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: ✅ 部署完成
        run: |
          echo "🎉 Survey 前端已部署到 GitHub Pages"
          echo "URL: ${{ steps.deployment.outputs.page_url }}"

