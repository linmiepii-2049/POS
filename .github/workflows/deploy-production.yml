name: Deploy to Production

on:
  workflow_dispatch:
    inputs:
      reason:
        description: '部署原因'
        required: true
        type: string
      version:
        description: '版本號（例如：v1.0.0）'
        required: true
        type: string

permissions:
  contents: write
  deployments: write

jobs:
  # 品質檢查
  pre-deploy-check:
    name: Production 部署前檢查
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🔍 執行完整檢查
        run: |
          cd packages/backend
          bash scripts/preflight.sh || true
          pnpm run test || true

      - name: 📋 驗證版本號格式
        run: |
          VERSION="${{ github.event.inputs.version }}"
          if [[ ! $VERSION =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ 版本號格式錯誤: $VERSION"
            echo "正確格式: v1.0.0"
            exit 1
          fi
          echo "✅ 版本號格式正確: $VERSION"

      - name: ✅ 品質檢查通過
        run: echo "🎉 Production 部署前檢查已通過！"

  # 部署後端
  deploy-backend:
    name: 部署後端到 Production
    runs-on: ubuntu-latest
    needs: pre-deploy-check
    environment:
      name: production
      url: https://pos-backend-prod.survey-api.workers.dev
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🗄️ 檢查 D1 資料庫狀態
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "檢查 Production D1 資料庫..."
          npx wrangler d1 info pos-db-prod || echo "⚠️ 資料庫資訊查詢失敗，但繼續執行"

      - name: 🔄 執行 D1 Migrations
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "檢查是否有 pending migrations..."
          if npx wrangler d1 migrations list DB --env production 2>&1 | grep -q "Migrations to be applied"; then
            echo "⚠️ 發現 pending migrations！"
            echo "Pending migrations:"
            npx wrangler d1 migrations list DB --env production
            echo ""
            echo "開始執行 Production migrations..."
            npx wrangler d1 migrations apply DB --env production
            echo "✅ Migrations 執行完成"
          else
            echo "✅ 所有 migrations 已是最新"
          fi

      - name: 📦 檢查 R2 Bucket 狀態
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "檢查 Production R2 bucket..."
          if npx wrangler r2 bucket list | grep -q "pos-assets-prod"; then
            echo "✅ R2 bucket 'pos-assets-prod' 存在"
          else
            echo "❌ R2 bucket 'pos-assets-prod' 不存在"
            echo "請先建立: npx wrangler r2 bucket create pos-assets-prod"
            exit 1
          fi

      - name: 💾 備份資料庫（可選）
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "📦 建議在部署前備份資料庫"
          echo "備份指令: npx wrangler d1 backup create pos-db-prod"
          echo "⚠️ 自動備份功能需要額外配置，此步驟僅為提醒"

      - name: 🚀 部署後端到 Production
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          GIT_SHA: ${{ github.sha }}
          VERSION: ${{ github.event.inputs.version }}
        run: |
          echo "開始部署到 Production..."
          echo "版本: $VERSION"
          echo "Commit: $GIT_SHA"
          npx wrangler deploy --env production
          echo "✅ Workers 部署完成"

      - name: 🔍 驗證部署
        run: |
          echo "等待部署生效..."
          sleep 30
          
          echo "測試健康檢查（重試 3 次）..."
          for i in 1 2 3; do
            if curl -f https://pos-backend-prod.survey-api.workers.dev/health; then
              echo "✅ 健康檢查成功！"
              break
            else
              echo "⚠️ 健康檢查失敗（第 $i 次），10 秒後重試..."
              sleep 10
            fi
          done
          
          echo "測試版本資訊..."
          curl -f https://pos-backend-prod.survey-api.workers.dev/version || echo "⚠️ 版本檢查失敗"
          
          echo "✅ 後端部署完成！"

  # 部署前端
  deploy-frontend:
    name: 部署前端到 Production
    runs-on: ubuntu-latest
    needs: deploy-backend
    environment:
      name: production
      url: https://pos-frontend-prod.pages.dev
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🔨 建置 SDK
        working-directory: packages/sdk
        run: |
          echo "開始建置 SDK..."
          pnpm run build
          echo "✅ SDK 建置完成"

      - name: 🏗️ 建置前端（Production）
        working-directory: packages/frontend
        env:
          VITE_API_BASE_URL: https://pos-backend-prod.survey-api.workers.dev
        run: |
          echo "VITE_API_BASE_URL=$VITE_API_BASE_URL" > .env.production
          pnpm run build

      - name: 🚀 部署到 Cloudflare Pages
        working-directory: packages/frontend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npx wrangler pages deploy dist \
            --project-name=pos-frontend-prod \
            --branch=main

      - name: ✅ 前端部署完成
        run: echo "🎉 前端部署成功！"

  # 建立 Git Tag
  create-tag:
    name: 建立版本標籤
    runs-on: ubuntu-latest
    needs: [deploy-backend, deploy-frontend]
    if: success()
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷️ 建立 Git Tag
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          VERSION="${{ github.event.inputs.version }}"
          
          # 檢查 tag 是否已存在
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "⚠️  Tag $VERSION 已存在，跳過建立"
          else
            git tag -a "$VERSION" -m "Release $VERSION - ${{ github.event.inputs.reason }}"
            git push origin "$VERSION"
            echo "✅ Tag $VERSION 已建立並推送"
          fi

  # 部署通知
  notify:
    name: 📢 Production 部署通知
    runs-on: ubuntu-latest
    needs: [deploy-backend, deploy-frontend, create-tag]
    if: always()
    
    steps:
      - name: 📊 部署結果
        run: |
          echo "================================"
          echo "🎉 Production 部署完成！"
          echo "================================"
          echo ""
          echo "🏷️  版本: ${{ github.event.inputs.version }}"
          echo "🔗 後端: https://pos-backend-prod.survey-api.workers.dev"
          echo "🔗 前端: https://pos-frontend-prod.pages.dev"
          echo ""
          echo "📋 部署資訊:"
          echo "  - 部署者: ${{ github.actor }}"
          echo "  - 部署原因: ${{ github.event.inputs.reason }}"
          echo "  - Commit: ${{ github.sha }}"
          echo "  - 分支: ${{ github.ref_name }}"
          echo "  - 時間: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo ""
          echo "✅ 請驗證 Production 環境"
          echo "================================"

