name: Survey CI - 品質檢查

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'packages/survey-frontend/**'
      - 'packages/sdk/**'
  push:
    branches: [main, develop]
    paths:
      - 'packages/survey-frontend/**'
      - 'packages/sdk/**'

jobs:
  survey-quality-check:
    name: Survey 程式碼品質檢查
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🔍 ESLint 檢查
        run: |
          cd packages/survey-frontend
          pnpm run lint

      - name: 🔍 TypeScript 型別檢查
        run: |
          cd packages/survey-frontend
          pnpm run typecheck

      - name: ✅ 品質檢查通過
        run: echo "🎉 Survey 前端品質檢查已通過！"

  survey-build-check:
    name: Survey 建置檢查
    runs-on: ubuntu-latest
    needs: survey-quality-check
    
    steps:
      - name: 📥 Checkout 程式碼
        uses: actions/checkout@v4

      - name: 📦 安裝 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🔧 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 安裝依賴
        run: pnpm install --frozen-lockfile

      - name: 🔨 建置 SDK（Survey 依賴）
        run: |
          cd packages/sdk
          pnpm run build

      - name: 🏗️ Survey 前端 - 一般建置
        run: |
          cd packages/survey-frontend
          pnpm run build

      - name: 🏗️ Survey 前端 - GitHub Pages 建置
        env:
          VITE_API_BASE: https://pos-backend-staging.survey-api.workers.dev
          VITE_LIFF_ID: 2007900041-O9ayn5JW
        run: |
          cd packages/survey-frontend
          echo "VITE_API_BASE=$VITE_API_BASE" > .env.production
          echo "VITE_LIFF_ID=$VITE_LIFF_ID" >> .env.production
          pnpm run build:gh-pages

      - name: 📊 檢查建置產物
        run: |
          echo "✅ 檢查建置產物..."
          ls -lah packages/survey-frontend/dist/
          echo "✅ 檢查 index.html 是否存在..."
          test -f packages/survey-frontend/dist/index.html && echo "✅ index.html 存在" || (echo "❌ index.html 不存在" && exit 1)
          echo "✅ 檢查 assets 目錄..."
          test -d packages/survey-frontend/dist/assets && echo "✅ assets 目錄存在" || (echo "❌ assets 目錄不存在" && exit 1)

      - name: ✅ 建置檢查通過
        run: echo "🎉 Survey 前端建置檢查已通過！"

