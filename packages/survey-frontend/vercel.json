{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @pos/sdk build && pnpm --filter @pos/survey-frontend build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

